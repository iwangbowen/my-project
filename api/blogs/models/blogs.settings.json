{
  "kind": "collectionType",
  "collectionName": "blogs",
  "info": {
    "name": "blogs"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "blog": {
      "collection": "file",
      "via": "related",
      "allowedTypes": [
        "images",
        "files",
        "videos"
      ],
      "plugin": "upload",
      "required": false
    },
    "json": {
      "type": "json"
    },
    "sum": {
      "type": "enumeration",
      "enum": [
        "a",
        "b",
        "c"
      ]
    }
  }
}
